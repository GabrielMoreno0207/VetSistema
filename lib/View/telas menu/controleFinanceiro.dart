import 'package:flutter/material.dart';

class ControleFinanceiroPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Controle Financeiro'),
      ),
      body: Center(
        child: Text('Página de Controle Financeiro'),
      ),
    );
  }
}
