import 'package:flutter/material.dart';

class ProcedimentosRealizadosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Procedimentos Realizados'),
      ),
      body: Center(
        child: Text('Página de Procedimentos Realizados'),
      ),
    );
  }
}
